program Fibo;

var
  head, tail, temp: int;

main {
  head = 0;
  tail = 1;
  print(head);
  
  if (head == 0) {
    print("head is ", head);
  };

  do {
    temp = head;
    head = tail;
    tail = temp + tail;
    print(head);
  } while(head < 100);
}

end